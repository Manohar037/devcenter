<html>
<h1><center>Listing articles</center></h1>
 <center>
<table><center>
  <tr>
    <th>Title</th>
    <th>Text</th>
    <th>Author</th>
    <th></th>
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
      <td><%= article.author %></td>
      <td><%= link_to 'Show', article_path(article) %></td>
    </tr>
  <% end %>
  </center>
</table>
 <%= link_to 'Create New Article', new_article_path(@article) %>
 </center>
</html>
